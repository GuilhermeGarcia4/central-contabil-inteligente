import { Component, Input, signal } from '@angular/core';
@Component({selector:'app-finance-help-term',template:`<span class="term"><span>{{label}}</span><button type="button" [attr.aria-expanded]="open()" (click)="open.set(!open())" [attr.aria-label]="'Entenda: '+label">?</button>@if(open()){<span class="explanation" role="note">{{explanation}}</span>}</span>`,styles:[`.term{position:relative;display:inline-flex;gap:6px;align-items:center}.term button{width:22px;height:22px;border:1px solid var(--line);border-radius:50%;background:#fff;color:var(--navy);cursor:pointer}.explanation{position:absolute;z-index:5;top:28px;left:0;width:min(280px,75vw);background:var(--navy);color:#fff;padding:10px;border-radius:8px;font-size:13px;font-weight:400;box-shadow:0 5px 18px #0002}`]})
export class FinanceHelpTermComponent{@Input({required:true})label='';@Input({required:true})explanation='';readonly open=signal(false)}
