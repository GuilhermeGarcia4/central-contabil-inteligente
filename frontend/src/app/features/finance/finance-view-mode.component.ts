import { Component, EventEmitter, Output, signal } from '@angular/core';
@Component({selector:'app-finance-view-mode',template:`<div class="mode" role="group" aria-label="Nível de detalhes"><button [class.active]="!detailed()" (click)="set(false)">Visão simples</button><button [class.active]="detailed()" (click)="set(true)">Ver detalhes</button></div>`,styles:[`.mode{display:flex;justify-content:flex-end;gap:4px;margin:10px 0}.mode button{padding:7px 10px;border:1px solid var(--line);background:#fff}.mode button:first-child{border-radius:8px 0 0 8px}.mode button:last-child{border-radius:0 8px 8px 0}.mode button.active{background:var(--navy);color:var(--accent)}`]})
export class FinanceViewModeComponent{@Output() detailedChange=new EventEmitter<boolean>();readonly detailed=signal(localStorage.getItem('finance-detail-mode')==='detailed');constructor(){queueMicrotask(()=>this.detailedChange.emit(this.detailed()))}set(value:boolean){this.detailed.set(value);localStorage.setItem('finance-detail-mode',value?'detailed':'simple');this.detailedChange.emit(value)}}
